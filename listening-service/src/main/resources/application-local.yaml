spring:
  jackson:
    time-zone: Asia/Ho_Chi_Minh
  application:
    name: listening-service
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
  kafka:
    bootstrap-servers: localhost:9094
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        max.request.size: 10485760
    consumer:
      group-id: listening-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
          spring.json.trusted.packages: "*"

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://identity.sep.local/realms/SEP490
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: Asia/Ho_Chi_Minh

    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://52.91.12.100:5432/listening_service
    username: phucth
    password: 123afd4a_sfd
    hikari:
      data-source-properties:
        prepareThreshold: 0
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000

server:
  port: 8071
  servlet:
    context-path: /listening


logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.hibernate: debug
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"

keycloak:
  base-uri: http://identity.sep.local
  token-uri: http://identity.sep.local/realms/SEP490/protocol/openid-connect/token
  realm: SEP490
  client-id: fptu_sep490
  client-secret: rsQ7Jml2bfMGkMcjKBLK515IZQff4JDz
  scope: openid
  redirect-uri: http://identity.sep.local/realms/SEP490/broker/google/endpoint

springdoc:
  paths-to-match: /**
  packages-to-scan: com.fptu.sep490.listeningservice.controller
  swagger-ui:
    path: /swagger-ui
    oauth:
      use-pkce-with-authorization-code-grant: true
      client-id: swagger-ui
  oauth-flow:
    authorization-url: http://identity.sep.local/realms/SEP490/protocol/openid-connect/auth
    token-url: http://identity.sep.local/realms/SEP490/protocol/openid-connect/token

topic:
  upload-audio: upload-audio-topic
  update-listening-audio: update-listening-audio-topic
  send-notification: send-notification-topic
  topic-master: topic-master
  gen-transcript: gen-transcript


cloudinary:
  cloud-name: dbrm5eowo
  api-key: 566182576423582
  api-secret: mYJD0DbR4oXl4oD8g22RtvJx9kU

assembly-ai:
  api-key: 783b1109a1e84578b4c88a202b2fa421
  url: https://api.assemblyai.com/v2/transcript

service:
  personal-service: http://localhost:8072/personal

webhook:
  base-url: https://tootsstore.online/api/v1